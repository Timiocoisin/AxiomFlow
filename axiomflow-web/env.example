### AxiomFlow Web - Environment Variables Example
### Copy to `.env.local` (same directory as `package.json`) and adjust values.
### Note: `.env.local` is git-ignored and will override `.env` if both exist.

# ---- OAuth Authentication ----
# Google OAuth 登录配置
# 在 Google Cloud Console (https://console.cloud.google.com/) 创建 OAuth 2.0 Client ID
# Application type: Web application
# Authorized JavaScript origins: http://localhost:5173 (开发环境)
# 获取到的 Client ID 格式类似: xxxx.apps.googleusercontent.com
VITE_GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"

